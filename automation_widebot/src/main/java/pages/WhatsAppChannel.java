package pages;

import org.openqa.selenium.WebDriver;

public class WhatsAppChannel {
    private WebDriver driver;

    public WhatsAppChannel (WebDriver driver){
        this.driver = driver;
    }
}
